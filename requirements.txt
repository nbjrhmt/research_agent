langgraph>=0.2.0
# LangGraph SqliteSaver(会话 checkpoint 持久化, 断点续研; 提供 langgraph.checkpoint.sqlite,
# 需要 langgraph-checkpoint>=4.1.0, 见 core/checkpoint_store.py)
langgraph-checkpoint-sqlite>=3.1.1
langchain>=0.3.0
langchain-openai>=0.2.0
# 联网搜索统一走博查(Bocha) Web Search API(已移除 ddgs/必应抓取方案, 不再需要 ddgs 包)
requests>=2.31.0
pypdf>=4.0.0
chromadb>=0.5.0
streamlit>=1.36.0
pandas>=2.0.0
matplotlib>=3.8.0
python-dotenv>=1.0.1
# token 计数(tiktoken): 素材按真实 token 预算裁剪, 替代纯字符截断
tiktoken>=0.7.0
# API 服务化(server.py): FastAPI 把 LangGraph Agent 封装为 REST 服务
fastapi>=0.110.0
uvicorn>=0.29.0
