你只能基于【已搜集资料】进行判断，不允许编造任何资料中不存在的信息。
用户原始需求：{user_query}
已搜集全部资料：{collected_info}

请依据资料完成判断：
1. 现有资料是否足够完成用户的调研需求(sufficient)；
2. 如果信息不足，列出还需要搜索的关键词(missing_topics, 最多5个)；
3. 如果信息足够，missing_topics 返回空数组。

严格只输出系统要求的 JSON 对象，不要输出任何其他文字。
